/target
# Release builds run in a container and write here as root (#70-era fix:
# the Linux binary is built natively, not cross-compiled).
/target-linux-release
/dist
/.kaeru-vault/
*.swp
.DS_Store
.idea/
.vscode/
.intentionally-empty-file.*

# Offline build: fetched by contrib/offline/fetch-vendor.sh, never committed
# here — the vendor tree lives in LamantinAI/kaeru-vendor, one tag per release.
/vendor/
/.cargo/config.toml
